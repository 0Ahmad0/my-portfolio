{
  "buildCommand": "cd artifacts/portfolio && npm install && npm run build:deploy",
  "outputDirectory": "artifacts/portfolio/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
