{
  "buildCommand": "cd artifacts/portfolio && pnpm install && pnpm exec vite build --config vite.config.deploy.ts",
  "outputDirectory": "artifacts/portfolio/dist",
  "installCommand": "npm install -g pnpm && pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
